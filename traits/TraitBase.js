export default class TraitBase {
	constructor(editor) {
		this.editor = editor;
	}

	createLabel({label}) {

	}

	createInput({ trait }) {

	}

	templateInput({trait}) {

	}

	onEvent({ elInput, component, event }) {

	}

	onUpdate({ elInput, component }) {

	}

}